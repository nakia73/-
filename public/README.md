# Public Assets

このフォルダ (`public/`) に置かれたファイルは、アプリケーションのルートパスから直接アクセスできます。

## ロゴの設定方法
1. このフォルダに `logo.svg` という名前でロゴ画像をアップロードしてください。
2. アプリのヘッダー部分に自動的に表示されます。
3. 画像サイズは横300px、縦100px程度のSVGが推奨されます（自動的にリサイズされます）。
